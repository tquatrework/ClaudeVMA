// This scaffold module has been superseded by profiles/ and relations/ modules.
// Kept as empty placeholder to avoid breaking the directory structure.
import { Module } from '@nestjs/common';

@Module({})
export class UserProfileModule {}
