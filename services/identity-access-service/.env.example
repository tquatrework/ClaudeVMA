NODE_ENV=development
PORT=3001

# PostgreSQL
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/identity_access

# JWT — change both secrets in production
JWT_SECRET=change-me-in-production-super-secret
JWT_EXPIRES_IN=1h
